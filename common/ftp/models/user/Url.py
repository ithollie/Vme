import os
class Urls(object):
	def __init__(self):
		pass
	@staticmethod
	def update_activation_status_url(indexOf):
		indexUrl = indexOf
		lenghtOfUrls = len(indexUrl)
		if lenghtOfUrls is None:
			return False
		else:
			return indexUrl
	@staticmethod
	
	#getPosition  = indexUrl[5:-1]
	#if getPosition != "htm":
	def mainPage(indexOf):
		indexUrl = indexOf
		lenghtOfUrls = len(indexUrl)
		#filenames = os.listdir("c://users/hacker/desktop/python/venv/templates")
		if lenghtOfUrls:
			return indexOf
		else:
			return False
		
